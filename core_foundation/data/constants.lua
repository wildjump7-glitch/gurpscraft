-- core_foundation/data/constants.lua
-- Engine-wide constants used across modules.

return {
    -- Default game tick rate (in seconds)
    TICK_RATE = 0.1,
    
    -- Default gravity multiplier relative to Luanti's gravity
    DEFAULT_GRAVITY_MULT = 1.0,
    
    -- Standard module prefix for naming and identification
    MODULE_PREFIX = "gurpscraft",
}